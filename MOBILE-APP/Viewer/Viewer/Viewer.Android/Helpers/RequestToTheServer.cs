using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace Viewer.Droid.Helpers
{
    class RequestToTheServer
    {
/*
        protected async override void SendRequest()
        {
           
            Device.BeginInvokeOnMainThread(async () =>
            {
                RestClient client = new RestClient();
                var wingetresult = await client.Get<Events>("url");
                if (wingetresult != null)
                {
                    //wingetresult.id.ToString(); //później przypiszemy to do LabeLChange

                }

            });

        }*/
    }
}